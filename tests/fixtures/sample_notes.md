# Gradient Descent Notes

Gradient descent is an optimization method for minimizing a loss function.

It updates parameters step by step in the opposite direction of the gradient.
The learning rate controls how large each update is.

Common mistakes include using a learning rate that is too large, forgetting to scale features, and treating convergence as guaranteed.

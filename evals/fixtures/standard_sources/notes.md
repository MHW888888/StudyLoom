# Optimization Starter Notes

Gradient descent is used to minimize a loss function by updating parameters with gradient information.

The learning rate controls the update size. A very large learning rate can make optimization unstable, while a tiny one can make progress slow.

Beginners should first understand loss functions, gradients, parameters, and iteration.
